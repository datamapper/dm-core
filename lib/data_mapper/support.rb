dir = Pathname(__FILE__).dirname.expand_path / 'support'

require dir / 'blank'
require dir / 'errors'
require dir / 'inflection'
require dir / 'kernel'
require dir / 'object'
require dir / 'pathname'
require dir / 'string'
require dir / 'struct'
require dir / 'symbol'
