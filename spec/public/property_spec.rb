require File.expand_path(File.join(File.dirname(__FILE__), '..', 'spec_helper'))

describe "Resource properties" do
  
  before(:each) do
    # Object.send(:remove_const, name) if defined?(Article)
  end
  
  describe "#attribute_get" do
    
    it "should be awesome"
    
  end
  
end